# TASK-011 完成报告

## 任务摘要

成功实现 TASK-011 规划任务与 Worker 功能，包含以下组件：

### 1. PlanningService (`lib/planning/planning-service.ts`)

- `createPlanningJob(projectId, userId)` - 创建 planning_jobs 记录，校验项目归属，应用层约束同一项目 pending/running 状态至多一条记录
- `getPlanningJobStatus(jobId, userId)` - 查询指定任务状态（含错误信息）
- `getLatestPlanningJobForProject(projectId, userId)` - 查询项目最新规划任务
- `markJobRunning(jobId, lockedBy)` - Worker 拉取时锁定任务
- `markJobCompleted(jobId)` - 标记任务完成
- `markJobFailed(jobId, errorMessage)` - 标记任务失败
- `incrementJobAttempt(jobId)` - 递增重试计数
- `setProjectPlanningReady(projectId, userId)` - 设置 `planning_ready=true`
- `upsertContentFileIndex(params)` - 创建/更新 content_files 索引记录

### 2. Worker (`lib/worker/planning-worker.ts`)

- `getWorkerId()` - 获取 Worker 实例 ID（分布式锁标识）
- `tryAcquireLock(jobId, workerId)` - 尝试获取任务锁
- `releaseLock(jobId)` - 释放任务锁
- `fetchPendingJob()` - 轮询获取待处理任务（按创建时间升序）
- `generateCharacterFile()` - 占位实现：生成 `00-人物档案.md`
- `generateOutlineFile()` - 占位实现：生成 `01-大纲.md`（含 7 列表格框架）
- `generateWritingPlanFile()` - 占位实现：生成 `02-写作计划.json`
- `executePlanningJob()` - 执行单个规划任务（生成三文件 + 索引 + 设置 planning_ready）
- `pollOnce()` - 单次轮询循环
- `runWorker(intervalMs, maxIterations)` - Worker 主循环

### 3. API 路由 (`app/api/projects/[projectId]/planning-jobs/route.ts`)

- `POST` - 创建规划任务（EARS-1）
- `GET` - 查询项目最新规划任务状态（用于轮询）

---

## EARS 覆盖矩阵

| EARS ID | 验收标准 | 实现位置 | 状态 |
|---------|----------|----------|------|
| REQ-008-AC-001 | 创建 planning_jobs 记录 | `planning-service.ts::createPlanningJob()` + API POST | ✅ |
| REQ-008-AC-005 | Worker 生成 00/01/02 文件 | `planning-worker.ts::executePlanningJob()` + 三生成函数 | ✅ |
| REQ-008-AC-007 | planning_ready 标志 | `planning-service.ts::setProjectPlanningReady()` + `upsertContentFileIndex()` | ✅ |

---

## 工程质量检查

| 检查项 | 命令 | 结果 |
|--------|------|------|
| TypeScript 类型检查 | `pnpm typecheck` | ✅ 零错误 |
| ESLint | `pnpm lint` | ✅ 零错误（仅 16 个 pre-existing warnings）|
| Build | `pnpm build` | ✅ 成功 |

---

## 技术约束遵守情况

| 约束 | 状态 |
|------|------|
| 使用 `@/lib/auth/auth` 中的 `auth()` 获取 session | ✅ |
| 使用 `@/lib/db/require-user.ts` 获取当前用户 | ✅ |
| 使用 `@/lib/storage/index.ts` 的 `createStorageDriver()` 写入 R2 | ✅ |
| storage_prefix 格式：`{userId}/{projectId}-{slug}/` | ✅（由 projects 表提供）|
| Worker 通过数据库轮询消费任务（不使用消息队列）| ✅ |
| 用户可见文案英文 | ✅ |
| 代码注释中文 | ✅ |
| 禁止 `as any` | ✅ |
| `pnpm typecheck` 零错误 | ✅ |
| `pnpm build` 成功 | ✅ |

---

## 文件清单

```
lib/planning/planning-service.ts    (新建)
lib/worker/planning-worker.ts      (新建)
app/api/projects/[projectId]/planning-jobs/route.ts  (新建)
```

---

## 后续工作（占位说明）

当前实现为占位实现（placeholder），三文件内容为结构化占位符，实际 AI 生成内容待后续集成 LLM 服务。

生成的内容：
- `00-人物档案.md` - 包含基础结构和"待补充"占位符
- `01-大纲.md` - 包含 7 列表格框架（章节数来自项目配置，默认 20）
- `02-写作计划.json` - 包含完整的章节结构（status: pending）

Worker 启动方式（待后续集成 Worker 基础设施）：
```typescript
import { runWorker } from '@/lib/worker/planning-worker';
await runWorker(5000, 0); // 5秒间隔，无限循环
```