# TASK-013 L4 规划确认与模式选择 - 完成报告

## 任务摘要

在 `/Volumes/data/NextCloud/coding/netx.js/feat-task-013` 目录下实现了 TASK-013 L4 规划确认与模式选择功能。

### 实现内容

#### 1. Server Actions (`app/actions/plan.ts`)
- `getPlanPageData(projectId)` - 获取 L4 规划确认页所需全部数据（项目信息、R2 大纲/写作计划/人物档案、已绑定/可用知识库）
- `getChapterSummaries(projectId, limit)` - 解析大纲文件提取前 N 章摘要
- `confirmPlan(input)` - 确认规划，选择写作模式（serial/parallel）与创作节奏（auto/manual），`creationPace=auto` 时创建 `generation_jobs` 记录，并更新项目状态为 `writing`

#### 2. 规划确认页 (`app/[locale]/(app)/projects/[projectId]/plan/page.tsx`)
- Server Component：登录校验 → 获取规划页数据 → 获取前5章摘要 → 渲染客户端组件

#### 3. 客户端交互组件 (`app/[locale]/(app)/projects/[projectId]/plan/PlanPageClient.tsx`)
- 前5章摘要卡片展示
- 全文大纲折叠/展开
- 知识库多选勾选（绑定参考文档）
- 写作模式选择器（serial/parallel）
- 创作节奏选择器（manual/auto）
- `creationPace=manual` 时强制 `serial` 逻辑
- 确认按钮提交 `confirmPlan` Server Action 并跳转创作进度页

## EARS 覆盖矩阵

| EARS ID | 需求 | 实现位置 | 状态 |
|---------|------|----------|------|
| EARS-1 | REQ-009-AC-001 规划预览页前5章摘要 | `getChapterSummaries()` + `ChapterSummaryCard` 组件 | ✅ |
| EARS-2 | REQ-009-AC-002 全文折叠可展开 | `outlineExpanded` state + 折叠按钮/展开内容 | ✅ |
| EARS-3 | REQ-009-AC-003 知识库勾选 | `selectedDocIds` state + 多选 checkbox 列表 | ✅ |
| EARS-4 | REQ-009-AC-004 serial/parallel + auto/manual 选择 | `ModeSelector` 组件 × 2，状态互锁逻辑 | ✅ |
| EARS-5 | REQ-009-AC-005 creationPace=auto → generation_jobs | `confirmPlan()` 中检测 `creationPace==="auto"` 并插入 `generation_jobs` | ✅ |

## 技术约束合规

| 约束 | 实现方式 |
|------|----------|
| 使用 `auth()` 获取 session | ✅ `import { auth } from "@/lib/auth/auth"` |
| 使用 `requireUser()` 获取当前用户 | ✅ `import { requireUser } from "@/lib/db/require-user"` |
| 使用 `getProjectDetail` | ✅ `import { getProjectService } from "@/lib/projects/project-service"` |
| 使用 R2 读取 02-写作计划.json | ✅ `createStorageDriver().readText(`${storagePrefix}02-写作计划.json`)` |
| 用户可见文案英文 | ✅ 所有 UI 文案英文 |
| 代码注释中文 | ✅ 中文注释标注 EARS 覆盖 |
| 禁止 `as any` | ✅ 未使用 |
| `pnpm typecheck` 零错误 | ✅ |
| `pnpm lint` 零错误 | ✅ (仅警告) |
| `pnpm build` 成功 | ✅ |

## 工程质量检查

```
pnpm typecheck: ✅ 零错误
pnpm lint:       ✅ 零错误（18 个警告均为其他文件，不影响本次实现）
pnpm build:      ✅ 成功，路由 /[locale]/projects/[projectId]/plan 正确生成
```

## 路由映射

- `planning_ready=true` → 跳转 L4 `/plan` (本实现)
- L4 确认后 → `writing` 状态 → `/projects/[projectId]/write` (创作进度页)

## 生成文件清单

- `app/actions/plan.ts` (12KB)
- `app/[locale]/(app)/projects/[projectId]/plan/page.tsx` (1.5KB)
- `app/[locale]/(app)/projects/[projectId]/plan/PlanPageClient.tsx` (11KB)