# TASK-006 完成报告

## 完成摘要

实现了中间件（Auth + i18n + 限流）：

1. **middleware.ts** — 增强未登录跳转 + callbackUrl；API 限流 100次/IP/小时超限返回 429；未登录 API 返回 401
2. **lib/rate-limit.ts** — 内存 Map 限流实现，getClientIp + checkRateLimit + rateLimitExceededResponse
3. **next-intl 集成** — `app/[locale]/layout.tsx` 配置 i18n，默认 locale zh；`i18n/request.tsx` 始终加载 en.json 错误文案
4. **messages/en.json** — `errors.*` 英文文案，`errors.rateLimited` 等
5. **sonner Toaster** — 全局 Toast 组件，非阻塞展示限流/错误
6. **next.config.ts** — 使用 `createNextIntlPlugin` 插件

## EARS 覆盖矩阵

| EARS ID | 描述 | 实现位置 |
|---------|------|----------|
| EARS-1 | middleware.ts 未登录跳转登录页 + callbackUrl | middleware.ts L53-58 |
| EARS-2 | API 速率超限返回明确英文错误 | middleware.ts L35-38 + lib/rate-limit.ts L66-77 |
| EARS-3 | 未登录 API 返回 401 | middleware.ts L40-47 |
| EARS-4 | 限流拒绝时 Server Actions/页面展示非阻塞英文错误 Toast | `app/[locale]/layout.tsx` Toaster + `messages/en.json` errors.rateLimited |

## 工程质量检查

```
pnpm typecheck  →  零错误 (tsc --noEmit 静默退出)
pnpm build      →  成功 (Next.js 16.2.6 Turbopack, 编译+类型检查+静态页面均通过)
```

- 禁止 `as any` ✅
- 代码注释中文 ✅
- 用户可见文案英文 ✅
- EARS 标注覆盖 4/4 ✅

## 文件变更

- `middleware.ts` — 重写，增强限流+auth
- `lib/rate-limit.ts` — 新增，限流逻辑
- `lib/i18n/index.ts` — 新增，i18n 配置
- `lib/i18n/routing.ts` — 新增，路由配置
- `i18n/request.tsx` — 新增，next-intl 请求配置
- `next.config.ts` — 更新，使用 next-intl 插件
- `app/[locale]/layout.tsx` — 新增，i18n locale 布局
- `app/[locale]/page.tsx` — 迁移，首页
- `app/layout.tsx` — 简化，根布局
- `messages/en.json` — 新增，英文错误文案